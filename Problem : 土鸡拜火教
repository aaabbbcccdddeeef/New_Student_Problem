#include<bits/stdc++.h>
 
int main() {
    int T;
    scanf("%d", &T);
    while (T--){
        int n, m;
        scanf("%d %d", &n, &m);
        int sum = n - m + 1;
        printf("%d\n", sum);
    }
    return 0;
}
